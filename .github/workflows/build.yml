name: Simple local
on: push
jobs:
  build-node:
    runs-on: ubuntu-latest
    cointainer: node:18
    steps:
      - run: node --version
      - run: npm --version
      - uses: actions/checkout@v3
      - run: npm install
      - run: npm install --save moment
      - run: node app.js